syntax = "proto3";
package chat;
option go_package = "https://github.com/spodolaks/gymkhana-main";

message Message{
    string body = 1;
}

service ChatService {
    rpc SayHello(Message) returns (Message){}
}